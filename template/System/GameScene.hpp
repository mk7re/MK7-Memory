#pragma once

#include "../types.hpp"

#include "RootScene.hpp"

BEGIN_NAMESPACE(System)
{
    // TODO: GameScene should inherit Scene, so inline getters should probably be pasted in here too
    /START_CLASS/NAME@GameScene/SIZE@0x310/BASE@RootScene/BSIZE@0x26C/
    public:
    /END/
}